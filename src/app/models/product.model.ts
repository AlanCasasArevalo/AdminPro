export class Product {
    constructor(
        public name?: string,
        public description?: string,
        public price?: number,
        public isActive?: boolean,
        public img?: string
    ) {}
}
